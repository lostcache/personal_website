---
import type { HTMLAttributes } from 'astro/types';
import { cn } from '../../lib/utils';

type Props = HTMLAttributes<'hr'>;

const { class: className, ...props } = Astro.props;
---

<hr class={cn('shrink-0 bg-border', className)} {...props} />
